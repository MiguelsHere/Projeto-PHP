Registar
Login

    Principal
Estatisticas do perfil (Login)
Exercicios (No Login)
Workouts pre feitos (No login)
Workouts criado pelo utilisador (Login)

    Extras
Calculadora de Calorias (No login)
Calculadora de dormir (No login)




        views
    -Pagina inicial (homepage)
    -Pagina de workouts
    -
    -


        Base de dados -> workout_db

    Tabelas
user
-id_user -> int(10) Primary key auto_increment
-user_name -> varchar(100) not null
-password -> (Fazer encriptação antes de guardar na base de dados)
-created_in -> datetime dafault current_date
-is_public -> bool not null (0/1) default 0
(user 0 vai ser o admin)

user_workout
-id_user_workout Primary key auto_increment
-id_user -> int(10) not null
-id_workout -> int(10) not null
-is_creator -> bool not null (0/1) default 0

workout
-id_workout -> int(10) Primary key auto_increment
-created_in -> datetime dafault current_date
-is_public -> bool not null (0/1) default 0

exercise
-id_exercise -> int(10) Primary key auto_increment
-exercise_name -> varchar(100) not null
-exercise_description -> varchar(1000) not null
-necessary_equipment -> varchar(1000)

exercise_workout
-id_exercise_workout -> int(10) Primary key auto_increment
-id_exercise -> int(10) foreing key
-id_workout -> int(10) foreing key


CREATE DATABASE workout_db

create table user(
    id_user int(10) PRIMARY KEY AUTO_INCREMENT,
    user_name varchar(100) NOT NULL,
    password ,
    created_in datetime DEFAULT CURRENT_DATE,
    is_public bool NOT NULL DEFAULT 0
);

create table user_workout(
    id_user_workout int(10) PRIMARY KEY AUTO_INCREMENT,
    id_user int(10) NOT NULL,
    id_workout int(10) NOT NULL,
    is_creator bool NOT NULL DEFAULT 0
);

create table workout(
    id_workout int(10) PRIMARY KEY AUTO_INCREMENT,
    id_user int(10) NOT NULL,
    created_in datetime DEFAULT CURRENT_DATE,
    is_public bool NOT NULL DEFAULT 0
);

create table exercise(
    id_exercise int(10) PRIMARY KEY AUTO_INCREMENT,
    exercise_name varchar(500) NOT NULL,
    exercise_description varchar(10000) NOT NULL,
    necessary_equipment
);

create table exercise_workout(
    id_exercise_workout int(10) PRIMARY KEY AUTO_INCREMENT,
    id_exercise int(10) NOT NULL,
    id_exercise_workout int(10) NOT NULL
);
